# Thinkus UI规格文档 (AI开发者优化版)

> 版本: 6.0.0 | 日期: 2026-01-10

---

## 1. 设计系统

### 1.1 设计令牌

> 详细定义见 `components/design-tokens.yaml`

```yaml
颜色:
  primary: 靛蓝 #6366F1 (专业)
  warm: 橙色 #F97316 (CTA按钮)
  success: #10B981
  warning: #F59E0B
  error: #EF4444
  gray: 50-900

字体:
  sans: Inter
  mono: JetBrains Mono

圆角:
  sm: 0.125rem
  md: 0.375rem
  lg: 0.5rem
  xl: 0.75rem
  2xl: 1rem
```

### 1.2 品牌角色

```yaml
名称: 小T
形象: 友好的AI助手
用途:
  - AI对话头像
  - 等待动画
  - 庆祝动画
情绪:
  - greeting: 打招呼
  - thinking: 思考中
  - celebrating: 庆祝
```

### 1.3 组件库

```yaml
基础: shadcn/ui
扩展:
  - AIChat: AI对话组件
  - ProgressHero: 进度展示
  - CelebrationHero: 庆祝组件
  - FeatureGrid: 功能网格
  - ExpertAvatar: 专家头像
```

---

## 2. 布局系统

### 2.1 布局类型

```yaml
marketing:
  用途: 首页、定价页
  特点: 全宽、无侧边栏
  
auth-center:
  用途: 登录、注册
  特点: 居中卡片、渐变背景
  
dashboard:
  用途: 仪表盘、项目页
  特点: 左侧边栏 + 主内容区
  
dashboard-minimal:
  用途: 创建页面
  特点: 简洁头部 + 全屏内容

celebration:
  用途: 完成页
  特点: 居中 + 庆祝效果
```

### 2.2 响应式断点

```yaml
sm: 640px   # 大手机
md: 768px   # 平板
lg: 1024px  # 小桌面
xl: 1280px  # 桌面
```

---

## 3. 页面规格

> 详细规格见 `pages/*.yaml`

### 3.1 首页 (/)

```yaml
目标: 让用户了解价值，产生兴趣

sections:
  - hero: 故事化标题 + 即时体验输入框
  - social-proof: 统计数字
  - product-types: 支持的产品类型
  - how-it-works: 四步流程
  - live-demo: 无需注册的AI体验
  - success-stories: 成功案例
  - pricing: 交互式价格配置器
  - faq: 常见问题
  - final-cta: 行动号召
```

### 3.2 登录页 (/login)

```yaml
目标: 快速登录

components:
  - logo + 标题
  - OAuth按钮 (Google, GitHub)
  - 分割线
  - 邮箱密码表单
  - 记住我 + 忘记密码
  - 注册链接
```

### 3.3 创建页 (/create) ⭐

```yaml
目标: 通过对话收集需求

layout: split-panel (60% + 40%)

left (对话区):
  - AI欢迎消息
  - 对话历史
  - 输入框 (支持语音、附件)
  - 快速模板

right (预览区):
  - 项目摘要卡片
  - 功能模块网格 (可编辑)
  - 价格图表
  - 确认按钮
```

### 3.4 确认页 (/create/confirm)

```yaml
目标: 用户确认方案

sections:
  - 项目摘要
  - 功能列表 (分组)
  - 技术栈展示
  - 价格明细 (可调整可选项)
  - 确认按钮
```

### 3.5 进度页 (/projects/[id]/progress) ⭐

```yaml
目标: 透明展示进度，缓解焦虑

sections:
  - 进度英雄区 (阶段 + 百分比 + ETA)
  
  - 左列:
    - 实时预览 (设备框架)
    - 开发日志 (自动滚动)
  
  - 右列:
    - AI团队状态
    - 等待任务 (域名、Logo、SEO)
    - 离开提示

特点:
  - SSE实时更新
  - 里程碑庆祝动画
  - 正向反馈 ("比预期快X%")
```

### 3.6 完成页 (/projects/[id]/complete) ⭐

```yaml
目标: 庆祝成功，引导下一步

sections:
  - 庆祝区 (彩带 + 产品预览)
  - 成就展示 (时间、页面数、节省金额)
  - 分享卡片 (生成分享图)
  - 下一步 (SEO、数据、新项目)

特点:
  - 彩带动画
  - 数字计数动画
  - 可分享成就图
```

### 3.7 仪表盘 (/dashboard)

```yaml
目标: 快速了解状态，快速操作

sections:
  - 欢迎区 (问候 + 快速创建)
  - 进行中项目 (突出显示)
  - 统计卡片
  - 最近项目网格
  - 快速操作
```

---

## 4. 组件规格

### 4.1 AIChat

```yaml
功能: AI对话组件

props:
  welcomeMessage: string
  inputPlaceholder: string
  voiceInput: boolean
  fileAttach: boolean
  templates: Template[]

slots:
  message: 自定义消息渲染
  input: 自定义输入框

events:
  onSend: (content: string) => void
  onFileAttach: (files: File[]) => void
```

### 4.2 ProgressHero

```yaml
功能: 进度展示

props:
  percentage: number
  currentStage: string
  eta: Date
  stages: Stage[]

features:
  - 阶段连接线
  - 当前阶段脉冲动画
  - 进度条平滑动画
```

### 4.3 FeatureGrid

```yaml
功能: 功能模块网格

props:
  features: Feature[]
  editable: boolean
  onToggle: (id: string) => void

feature_card:
  - 图标
  - 名称
  - 描述
  - 价格 (可选)
  - 开关 (如果可编辑)
```

### 4.4 ExpertPanel

```yaml
功能: 专家讨论面板

props:
  discussion: Discussion
  onInput: (content: string) => void

features:
  - 专家头像列表
  - 消息气泡
  - 打字指示器
  - 用户输入框
  - 跳过/暂停按钮
```

### 4.5 CelebrationHero

```yaml
功能: 庆祝组件

props:
  title: string
  confetti: boolean
  duration: number

features:
  - 彩带粒子效果
  - 标题缩放动画
  - 持续时间可配置
```

---

## 5. 交互规范

### 5.1 微交互

```yaml
按钮:
  hover: scale(1.02)
  click: scale(0.98)
  duration: 150ms

卡片:
  hover: translateY(-4px) + shadow-lg
  duration: 200ms

输入框:
  focus: ring-2 ring-primary-200
  duration: 150ms
```

### 5.2 页面转场

```yaml
进入: fade-in + slide-up, 300ms
退出: fade-out, 200ms
```

### 5.3 加载状态

```yaml
按钮: 禁用 + spinner
内容: skeleton shimmer
全屏: 中心spinner + 文字
```

### 5.4 反馈

```yaml
成功: checkmark动画 + toast
错误: shake动画 + toast
警告: 黄色toast
```

### 5.5 庆祝效果

```yaml
触发: 里程碑完成、项目完成
效果: confetti粒子爆发
持续: 2-3秒
```

---

## 6. 动效规范

### 6.1 缓动函数

```yaml
默认: ease-out
弹性: cubic-bezier(0.34, 1.56, 0.64, 1)
```

### 6.2 持续时间

```yaml
instant: 100ms (点击反馈)
fast: 150ms (hover)
normal: 200ms (过渡)
slow: 300ms (页面转场)
```

### 6.3 Framer Motion

```jsx
// 页面进入
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3 }}
/>

// 列表错开
<motion.div
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  transition={{ delay: index * 0.1 }}
/>

// 庆祝彩带
// 使用 canvas-confetti 库
```

---

## 7. 图标和插画

### 7.1 图标

```yaml
库: Lucide React
大小:
  sm: 16px
  md: 20px
  lg: 24px
  xl: 32px
```

### 7.2 Emoji

```yaml
用途: 功能图标、状态指示
示例:
  - 🌟 品牌
  - 👨‍💼 产品经理
  - 🎨 设计师
  - 👨‍💻 开发者
  - 🎉 庆祝
  - ✅ 完成
```

---

## 8. 移动端适配

### 8.1 布局调整

```yaml
sidebar:
  desktop: 固定左侧
  mobile: 底部导航栏

创建页:
  desktop: 左右分栏
  mobile: 单列 + 底部抽屉预览

进度页:
  desktop: 左右分栏
  mobile: 标签页切换
```

### 8.2 触摸优化

```yaml
点击区域: >= 44px
间距: >= 8px
手势:
  - 下拉刷新
  - 左滑删除
```

---

## 9. 无障碍

```yaml
ARIA:
  - 所有交互元素有label
  - 使用语义化HTML

键盘:
  - Tab导航
  - Enter/Space激活
  - Esc关闭模态框

颜色:
  - 对比度 >= 4.5:1
  - 不只靠颜色传达信息
```

---

## 8. 提示词仪表盘 (内部) ⭐ NEW

### 8.1 总览页

```yaml
路由: /admin/prompts
权限: 内部管理员

布局:
  - 总体健康度卡片 (大数字+状态)
  - 核心指标网格 (4列)
  - 趋势图表 (7天)
  - 需要关注的提示词列表
  - 进行中的A/B测试
  - 最近优化记录

关键指标:
  - 总体健康度: 0-100 + 🟢🟡🟠🔴
  - 格式正确率: 98.5%
  - 任务成功率: 95.2%
  - 平均Token: 1,250
  - 返工率: 3.2%
```

### 8.2 单提示词详情

```yaml
路由: /admin/prompts/:id
权限: 内部管理员

布局:
  header:
    - 提示词名称 + 当前版本
    - 综合评分 (圆环图)
    - 快速操作按钮

  tabs:
    - 概览: 核心指标 + 趋势
    - 版本: 版本列表 + 对比
    - 执行: 最近执行记录
    - 失败: 失败样本分析
    - 审核: 审核历史

  sidebar:
    - 健康度状态
    - 下次评估时间
    - 操作按钮
```

### 8.3 指标可视化

```yaml
图表类型:
  健康度: 仪表盘 (Gauge)
  趋势: 折线图 (Line)
  分布: 饼图 (Pie)
  对比: 柱状图 (Bar)

颜色编码:
  优秀: #10B981 (绿)
  良好: #6366F1 (蓝)
  一般: #F59E0B (黄)
  较差: #EF4444 (红)
```

---

## 附录: 页面文件索引

```yaml
# 用户端
pages/_index.yaml: 页面列表
pages/login.yaml: 登录页
pages/register.yaml: 注册页
pages/create-chat.yaml: 创建页 ⭐
pages/create-confirm.yaml: 确认页
pages/progress.yaml: 进度页 ⭐
pages/complete.yaml: 完成页 ⭐
pages/dashboard.yaml: 仪表盘

# 内部管理
pages/admin/prompts.yaml: 提示词仪表盘 ⭐ NEW
pages/admin/prompts-detail.yaml: 提示词详情 ⭐ NEW

# 设计系统
components/design-tokens.yaml: 设计令牌
```
